{
  "name": "hysteria-tracker",
  "version": "1.0.0",
  "description": "Hysteria visitor tracker",
  "main": "api/track.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed'"
  },
  "dependencies": {
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
